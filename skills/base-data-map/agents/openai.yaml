interface:
  display_name: "Base / Mapa De Datos"
  short_description: "Traza datos por metodo y renderiza visual por archivo/metodo"
  default_prompt: "Usa $base-data-map para generar DataMap.md y DataMap.visual.html con cajones por archivo y filas por metodo (recibe-metodo-envia)."
policy:
  allow_implicit_invocation: true
